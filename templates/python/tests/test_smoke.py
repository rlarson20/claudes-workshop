"""Starter test.

Delete this once real tests exist. It fails on purpose so the TDD loop starts
in the RED state: write real failing tests from SPEC.md, then implement to green.
"""

import importlib


def test_package_imports():
    # Sanity check that the package is importable and installed.
    importlib.import_module("{{PACKAGE_NAME}}")


def test_replace_me():
    # RED by design. Replace with real tests derived from SPEC.md.
    raise AssertionError("Write real tests from SPEC.md, then implement to green.")
